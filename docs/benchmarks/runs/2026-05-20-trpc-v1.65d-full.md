# Run 2026-05-20 — tRPC Full Bench v1.65d (Qwen3-35B MoE)

## Configuration

| | |
|---|---|
| Date | 2026-05-20 |
| rag-system revision | `085864f` (v1.65d) |
| LLM_LARGE_MODEL | `qwen3-32k` (Qwen3-35B MoE, 32K ctx, thinking mode) |
| trpc rev | `23c723c` (v11.17.0) |

## Results

| # | Task | Result | Commit | Notes |
|---|------|--------|--------|-------|
| T1 | JSDoc TRPCError | ✅ commit | `a908736` | Clean JSDoc |
| T2 | getHTTPStatusCode helper | ✅ commit | `53c9d45` | Repo memory helps import path |
| T3 | onError callback in standalone | ✅ commit | `78abb93` | **First consistent pass!** Used `HTTPErrorHandler` type from trpc internals |
| T4 | createTimeout AbortSignal | ✅ commit | `42ba193` | Clean |
| T5 | maxBodySize nodeHTTPRequestHandler | ✅ commit | `042dc6f` | Correct opts.maxBodySize usage |
| T6 | dataLoader retry | ❌ reviewer_reject | — | Created new file but retry logic incorrect |

**tRPC Qwen3 MoE v1.65d: 5/6 (83%)** — T3 now passes, T6 fails (variance swap)

---

## vs v1.65c

| | v1.65c | v1.65d | Change |
|---|---|---|---|
| T1 | ✅ | ✅ | = |
| T2 | ✅ | ✅ | = |
| T3 | ❌ reviewer_reject | ✅ | **T3 FIXED** |
| T4 | ✅ | ✅ | = |
| T5 | ✅ | ✅ | = |
| T6 | ✅ (new file) | ❌ reviewer_reject | variance swap |
| **Total** | **5/6** | **5/6** | same score, different failure |

---

## T3 success analysis

T3 passed with commit `78abb93`. The Coder:
1. Used `add_type_member` to add `onError?: HTTPErrorHandler<TRouter, TRequest>` to `StandaloneHandlerOptions` intersection type — v1.65d's intersection support was key
2. Also modified `nodeHTTPRequestHandler.ts` (4 lines) to wire the runtime call
3. Used `HTTPErrorHandler` from `'../unstable-core-do-not-import/http/types.js'` — the CORRECT internal type, not a made-up one

The v1.65d prompt rule "ALSO wire runtime if task describes behavior" guided the Coder to make both changes. The intersection type support in `add_type_member` allowed clean type modification without refactoring generics.

## T6 variance

T6 creates `packages/server/src/dataLoader.ts` (prompt specifies this non-existent path). In v1.65c, the retry logic was correct → ✅. In v1.65d, the Reviewer rejected "retry logic incorrect" → ❌. Pure model variance — same prompt, different LLM temperature sampling.

The `add_type_member` tool was not used for T6 (the type was part of the new file creation).

---

## System status after v1.65d

Failures now rotate between T3 and T6 — both are model variance:
- T3 is now solvable (intersection support + prompt guidance)
- T6 needs better retry algorithm in the generation (or a fixed prompt to the existing client file)

5/6 (83%) is confirmed stable on trpc.
