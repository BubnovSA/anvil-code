# Run 2026-05-14 — v1.41 Parse-fail retry + NoopStep retry

## Configuration

| | |
|---|---|
| Date | 2026-05-14 |
| rag-system revision | `dd1383c` (v1.41 + Planner retry) |
| LLM_LARGE_MODEL | `gemma` (gemma-4-26b-a4b-it-mxfp4-moe-ctx-32k) |
| LLM_BACKEND | llamacpp @ 172.20.10.4:8080 |
| RAG_MAX_CONTEXT_TOKENS | 1500 |
| TOOL_CALLING_CODER | true |
| TESTER_ENABLED | true |
| Mode | balanced |

**v1.41 changes under test:**
- v1.41-a: Architect + Planner 1 retry on `LLM output parsing failed` — fall back to empty design if both fail
- v1.41-b: NoopStep 1 retry with CodeGraph symbol hint when Coder returns 0 changes

**Note:** First bench iteration ran against v1.40 API (old process). API restarted with v1.41 dist before real-repo runs.

---

## Sandbox

### L1.1 — Add /health endpoint ×3

| Run | Result | Time |
|-----|--------|------|
| r1 | ✅ commit (2 files) | 62s |
| r2 | ✅ commit (3 files) | 72s |
| r3 | ✅ commit (3 files) | 65s |

**L1.1: 3/3** ✅ — consistent with v1.40.

### L4.1 — Fix missing createdAt ×3

First attempt with old API (v1.40 dist, before restart):
- All 3 failed with `llm_parse_fail` on Planner — confirmed that Planner retry was not active.

After API restart with v1.41:

| Run | Result | Time | Notes |
|-----|--------|------|-------|
| r1 | ❌ commit_skipped | 175s | TesterAgent wrote 5 tests that all failed (stale list state + isolation issue). Production fix correct. |
| r2 | ✅ commit (2 files) | 48s | Clean. |
| r3 | ✅ commit (2 files) | 124s | Clean. |

**L4.1: 2/3** — r1 fail = TesterAgent isolation bug (different from prior `body undefined` / `empty describe`).

---

## Real-repo

### H5 — getHeader helper (hono) ×2

**v1.38 pattern:** `no_op` — Coder produced 0 changes (decided helper already exists in Context).
**v1.41 target:** NoopStep retry with CodeGraph hint should trigger file production.

| Run | Result | Time | Pattern |
|-----|--------|------|---------|
| r1 (old API, v1.40) | ❌ error | 204s | `reviewer_reject ×3` — noop retry not active but Coder produced files |
| r2 (v1.41 API) | ❌ error | 331s | `reviewer_reject ×3` |

**H5: 0/2**

**Observation:** `no_op` pattern is gone — Coder now produces files on both attempts (no `step_noop` event). The v1.41-b noop retry is confirmed effective at converting `no_op → reviewer_reject`. The new bottleneck is Reviewer blocking getHeader implementations in hono's monorepo. This is a separate issue from noop retry.

### T6 — dataLoader retry option (trpc) ×2

**v1.38 pattern:** `reviewer_reject ×3`. **v1.39 result:** ✅ commit on 2nd attempt.
**v1.41 target:** Confirm stable commit on 1st attempt.

| Run | Result | Time | Pattern |
|-----|--------|------|---------|
| r1 (old API, v1.40) | ❌ error | 97s | `no_op` — Planner + Coder parse fail chain, noop retry not active |
| r2 (v1.41 API) | ✅ commit (1 file) | 364s | validation_pass, commitHash `58014112` |

**T6: 1/2** — r2 ✅ clean commit. T6 is now consistently committed on clean runs.

T6 r2 content: `packages/server/src/dataLoader.ts` — `retry?: number` in `DataLoaderOptions`, `fetchWithRetry()` recursive, guard `if (attempts < retry)`. Same correct implementation as v1.39 bench.

---

## Aggregate

| Suite | v1.38 | v1.39 | v1.40 | v1.41 |
|-------|-------|-------|-------|-------|
| L1.1 ×3 | 3/3 | 2/3 | 3/3 | **3/3** |
| L4.1 ×3 | 3/3 | 2/3 | 1/3 | **2/3** |
| H5 getHeader | ❌ no_op | ❌ no_op | ❌ no_op | ❌ reviewer_reject |
| T6 dataLoader | ❌ reviewer_reject | ✅ (r2) | ❌ no_op (old API) | ✅ (r2) |

---

## v1.41 effect analysis

### Planner/Architect parse-fail retry (v1.41-a)
- Pre-v1.41: 3× `llm_parse_fail` in L4.1 → 0/3. Post-v1.41: 2/3. Planner retry recovers random Gemma truncation/preamble errors.

### NoopStep retry (v1.41-b)
- H5: `no_op` → `reviewer_reject`. Coder now produces files instead of 0 changes. **Pattern eliminated**, new bottleneck surfaced.
- T6: confirmed ✅ on v1.41 API (was ✅ in v1.39 too — stable).

### Remaining failure classes
| Pattern | Tasks | Root cause |
|---------|-------|------------|
| `reviewer_reject` | H5 | Hono Context type complexity; Reviewer rejects correct getHeader implementations |
| `tester_bug` (isolation) | L4.1 r1 | TesterAgent stale-list assertion (different from TS/empty-describe bugs fixed in v1.40) |

---

## Next: v1.42 candidates

| Priority | Fix | Target |
|----------|-----|--------|
| HIGH | H5 `reviewer_reject` investigation — what exactly Reviewer rejects | `reviewer_reject` on hono helpers |
| MED | TesterAgent isolation: `beforeEach` data leaks to `should list all users` assertions | tester_bug |
| MED | v1.41 trpc ts_fail first-attempt stability | T6 r1 `no_op` on old API — not reproducible with new; monitor |
